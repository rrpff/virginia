{
  "name": "virginia",
  "scripts": {
    "build": "run-s 'build -w packages/core' 'build -w apps/server'",
    "dev": "run-p 'dev -w apps/server' 'dev -w packages/core'",
    "start": "run-p 'start -w apps/server'"
  },
  "workspaces": [
    "apps/server",
    "packages/core",
    "packages/tsconfig"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
